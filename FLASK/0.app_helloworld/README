# PASOS PARA CREAR UN PROYECTO( APP WEB ) EN PYTHON FLASK 
# USANDO UN ENTORNO VIRTUAL E INSTALACION SUS DEPENDENCIAS(PAQUETES)

# PASO 1, Crear mi entorno virtual

python3 -m venv env

ó

python -m venv env

#PASO 2, Activar el entorno virtual ejecutando

. env/Scripts/activate

#PASO 3, Ya dentro del entorno virtual instalar flask y todas sus dependencias

pip install Flask 
pip install Flask-SQLAlchemy
pip install SQLAlchemy

#PASO 4, Crear/Actualizar el fichero requirements.txt:

pip freeze > requirements.txt

#PASO 5, para correr el proyecto solo debes ejecutar el archivo
# esto se realiza cuando voy a instalar en otra carpeta o dispositivo

pip install -r requirements.txt

#PASO 6, Para desactivar nuestro entono virtual

deactivate

#comando para actualiza pip ( opcional )

python -m pip install --upgrade pip
